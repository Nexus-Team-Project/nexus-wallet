# MapTiler API key — used by the OffersMap component for vector tiles.
# Get a free key (100K loads/month) at https://www.maptiler.com/
# When omitted, the map falls back to OpenFreeMap's "positron" style
# (no API key needed, public infra).
VITE_MAPTILER_KEY=
